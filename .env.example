# The shared Claude proxy (homielea/fable-ai-proxy). Leave unset for the
# offline fallback. Build-time vars — restart expo after changing.
EXPO_PUBLIC_PROXY_URL=https://your-proxy.example
# Must match the proxy's PROXY_TOKEN when it has one set.
EXPO_PUBLIC_PROXY_TOKEN=
